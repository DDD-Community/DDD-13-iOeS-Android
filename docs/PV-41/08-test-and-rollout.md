# PV-41 — 구현 순서와 테스트 계획

> **Stub 단계는 끝났다(2026-09-04).** Stub 관련 절은 진행 방식의 기록이다 —
> 모든 Service 는 `Default*` 실서버 구현이고, 검수 결과만 로컬 status 대조를 쓴다.

## 구현 순서

### 1. 도메인과 Stub Backend

1. 도메인 모델에 다섯 상태와 출처·반려·추천·북마크·검수 결과를 정의한다.
2. Service 인터페이스를 UI 요구사항 기준으로 정의한다.
3. 공유 `StubSpotBackend`와 Stub Service를 `@Singleton`으로 구현한다.
4. 성공·실패·지연·경합 fixture와 각 상태의 초기 데이터를 추가한다.

### 2. 작성자 상태 전환

1. 상세 ViewModel의 의존성 책임을 정리한다.
2. 페이크도어와 `notifyUpdateRequested()`를 제거한다.
3. 신청·철회·반려 철회·오픈 취소·삭제를 구현한다.
4. 확인 모달, 실패 토스트, 동시 처리 새로고침을 구현한다.

### 3. 반려 수정·재신청

1. 편집 상세로 등록 폼을 프리필한다.
2. 기존 이미지 유지와 교체를 분리한다.
3. 재신청 모달과 `RE_REVIEW_PENDING` 전환을 구현한다.

### 4. 공개 경험

1. 출처와 추천 상태를 공개 상세에 연결한다.
2. 추천·취소, 로그인 가드, 중복 탭 방지, 실패 롤백을 구현한다.
3. 공개 유저 스팟을 공용 지도·리스트·클러스터에 포함한다.

### 5. 보관함과 결과 알림

1. 비공개 북마크 카드와 단일 삭제 안내를 구현한다.
2. 보관 탭 인디케이터와 승인·반려 스낵바를 구현한다.
3. 승인 후 첫 상세 진입 모달을 구현한다.

### 6. API 명세 반영과 Integration

1. 각 기능 문서의 API 계약 입력란을 서버 명세로 채운다.
2. DTO·Retrofit API·Mapper·`Default*Service`를 작성한다.
3. Stub 응답 fixture를 실제 response fixture로 교체·확장한다.
4. `ServiceModule` 바인딩을 Stub에서 Default 구현으로 전환한다.
5. 서버 개발 환경에서 정상·오류·경합 응답을 검증한다.
6. ViewModel과 Compose UI에 연동 전용 분기가 추가되지 않았는지 확인한다.

## Stub 단계 완료 기준

- 서버 없이 등록 → 신청 → 승인/반려 → 재신청/오픈 취소 전체 흐름을 재현할 수 있다.
- 상태 변경이 상세, 지도, 리스트, 보관함에 일관되게 반영된다.
- 추천과 비공개 북마크가 공유 인메모리 상태를 사용한다.
- 네트워크 지연에 대응하는 로딩·중복 탭 방지 UI를 확인할 수 있다.
- 일반 실패와 철회/검수 경합 UI를 개발 메뉴 또는 테스트 fixture로 재현할 수 있다.
- ViewModel 테스트에서 Stub Service 대신 Mock Service도 주입할 수 있다.

## Integration 단계 완료 기준

- 모든 기능 문서의 API 계약 상태가 `VERIFIED`다.
- 실제 response fixture의 DTO·Mapper 테스트가 존재한다.
- Stub과 Default Service가 같은 도메인 결과와 오류 의미를 제공한다.
- Hilt 바인딩 전환 외에 ViewModel·UI 변경이 없다.
- 서버 개발 환경에서 상태 전이, 추천 최종 수, 비공개 북마크, 결과 확인을 검증한다.
- 서버 미지원 필드나 임시 fallback이 남으면 코드와 문서에 제거 조건을 기록한다.

## Phase A — ViewModel·서비스

- 등록 결과가 `DRAFT`로 매핑된다.
- 다섯 상태가 손실 없이 매핑된다.
- 신청·철회·재신청·오픈 취소 성공 시 서버 최종 상태를 반영한다.
- 실패 시 이전 상태와 버튼 활성 상태를 복구한다.
- 철회/검수 경합 시 안내 후 최신 상세를 조회한다.
- 추천 성공 시 서버 최종 카운트를 사용하고 실패 시 롤백한다.
- 연속 추천 탭이 한 요청으로 제한된다.
- 비로그인 추천은 서버 요청 없이 로그인 유도 상태를 발생시킨다.
- 비공개 북마크 삭제 실패 시 카드가 원래 위치에 복구된다.
- 검수 결과 확인 전후 인디케이터가 올바르게 바뀐다.

## Phase B — Compose UI

- 상태별 CTA가 명세와 일치한다.
- 모달 카피와 확정·보조 버튼 동작이 일치한다.
- 반려 사유와 두 액션이 표시된다.
- 재신청 폼에 기존 값이 채워지고 작성 중 값이 유지된다.
- 공개 상세에만 추천과 피드백이 표시된다.
- 큐레이션 출처와 유저 등록 뱃지가 구분된다.
- 비공개 북마크는 상세 대신 단일 삭제 안내를 연다.
- 보관 탭 인디케이터, 결과 스낵바, 승인 완료 모달이 동작한다.

## Phase C — Paparazzi·지도

- `DRAFT`, 검수중, 반려, 공개, 큐레이션 상세
- 반려 배너와 신청·철회·재신청·오픈 취소·삭제 모달
- 추천 활성·비활성과 긴 숫자 카운트
- 비공개 북마크 카드와 보관 탭 인디케이터
- 회색 MY 핀, 일반 공개 핀, 선택 주황 링, 혼합 클러스터
- 라이트·다크와 주요 접근성 폰트 크기

## 통합 QA

- [ ] 등록 직후 검수 없이 나만보기로 저장된다.
- [ ] 최초 신청과 재신청 상태가 구분되지만 UI는 모두 검수중이다.
- [ ] 신청 철회, 반려 철회, 오픈 취소가 `DRAFT`로 복귀한다.
- [ ] 반려 사유 확인과 기존 값 보완·재신청이 가능하다.
- [ ] 오픈 취소 후 재승인해도 추천 수가 유지된다.
- [ ] 공개 유저 스팟만 공용 지도·리스트·클러스터에 노출된다.
- [ ] 상세 출처가 큐레이션과 유저 등록 규칙에 맞는다.
- [ ] 추천·취소, 셀프 추천, 실패 롤백이 동작한다.
- [ ] 비공개 북마크가 상태 카드로 남는다.
- [ ] 공개 유저 상세에 기존 피드백 링크가 노출된다.
- [ ] 유저 스팟 삭제와 탈퇴 데이터 유지 정책을 검증한다.
- [ ] 승인·반려 결과 안내와 인디케이터가 동작한다.
- [ ] 네트워크 실패와 동시 처리 예외가 QA를 통과한다.

## 완료 기준과 유저 시나리오 매핑

기획 §5 완료 기준에 번호를 붙이고(위에서부터 1~15) 각 유저 시나리오가 무엇을 닫는지 정리한다.
세부 케이스 ID는 `ui-test-cases.md`를 따른다.

### 완료 기준 번호

1. 나만보기 → 검수중 → 오픈/반려 상태 전환과 3.7 표 화면 표시
2. 오픈 신청 확인 모달 확정 카피와 신청 접수 토스트
3. 검수중 신청 철회와 철회-동시처리 예외
4. 반려 사유 배너와 기존 입력값이 채워진 재신청 폼
5. 오픈 취소와 추천 수 유지
6. 모든 공개 상세의 출처 표기 줄 고정 노출
7. 큐레이션 출처 줄과 유저오픈 `유저 등록` 뱃지
8. 공개 유저 스팟의 지도·클러스터·리스트 노출과 선택 주황 링
9. 추천 탭·취소, 실시간 카운트, 셀프 추천, 큐레이션 추천
10. 북마크 시점 사전 안내와 비공개 전환 후 보관함 상태 표시
11. 유저오픈 상세의 기존 피드백 링크
12. 작성자 탈퇴 시 스팟·좋아요 데이터 유지
13. 유저 등록 스팟 삭제
14. 오픈 승인 시 스낵바·저장 아이콘 인디케이터·첫 진입 모달
15. 4.1·4.2 예외 케이스

### 시나리오별 커버리지

| 시나리오 | 완전 검증 | 부분 검증 | 테스트 ID |
| --- | --- | --- | --- |
| ① 오픈 신청~승인 | 2, 14 | 1(DRAFT→PENDING→PUBLISHED와 검수중 지도 미노출), 8(승인 후 노출까지만) | DTL-04, DTL-05, DTL-06, DTL-18, REV-01~03, MAP-03 |
| ② 반려·재신청 | 4 | 1(PENDING→REJECTED→RE_REVIEW_PENDING) | DTL-07, DTL-09, DTL-11, REG-04~06 |
| ③ 오픈 취소 | 5 | 1(PUBLISHED→DRAFT 복귀) | DTL-13 |
| ④ 발견·추천·북마크 | 7 | 6(유저오픈 상세만), 8(지도 발견 경로), 9(추천 탭만), 10(사전 안내) | DTL-12, DTL-16, MAP-04, MAP-05 |
| ⑤ 비공개 전환 보관함 | 없음 | 10(비공개 카드와 단일 삭제), 5(취소 결과의 탐색자 반영) | ARC-04, ARC-05 |

- 1은 ①+②+③을 합쳐야 다섯 상태와 3.7 표를 모두 덮는다.
- 10은 ④의 사전 안내와 ⑤의 사후 상태 표시를 합쳐야 닫힌다.

### 시나리오가 덮지 못하는 완료 기준

| 번호 | 항목 | 필요한 검증 |
| --- | --- | --- |
| 3 | 신청 철회와 경합 | 검수중 철회로 DRAFT 복귀, 철회와 승인 동시 발생 시 안내 후 최신 상세 재조회 |
| 11 | 기존 피드백 링크 | ④에 피드백 진입 단계 추가로 흡수 가능 |
| 12 | 탈퇴 데이터 유지 | 앱 시나리오로 재현 불가, 서버 QA 항목으로 분리 |
| 13 | 유저 스팟 삭제 | ③ 변형으로 오픈 취소 대신 삭제 확인 |
| 15 | 4.1·4.2 예외 | 네트워크 실패 롤백, 중복 탭 방지, 비로그인 추천 |

부분 커버로 남는 조각은 6의 큐레이션 상세 출처 줄, 8의 혼합 클러스터와 선택 주황 링,
9의 추천 취소·셀프 추천·큐레이션 추천이다.

### 보완 시나리오 제안

- ⑥ 등록자 — 철회와 경합: 검수중 철회로 DRAFT 복귀, 철회 시점에 이미 승인된 경우 안내 후 최신 상세 조회 → 3
- ⑦ 탐색자 — 큐레이션 스팟: 큐레이션 상세 출처 확인, 추천과 취소로 실시간 카운트 확인, 혼합 클러스터에서 선택 링 확인 → 6, 8, 9
- ⑧ 등록자 — 삭제와 실패: 공개 스팟 삭제, 네트워크 차단 후 추천·삭제 롤백 확인 → 13, 15

## 검증 명령

```bash
./gradlew :app:testDebugUnitTest
./gradlew :app:verifyPaparazziDebug
./gradlew assembleDebug
```
