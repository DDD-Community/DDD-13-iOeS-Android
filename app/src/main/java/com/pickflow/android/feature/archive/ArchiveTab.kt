package com.pickflow.android.feature.archive

/** iOS `ArchiveTab` 1:1 — 보관함 화면의 2-탭(저장된 스팟 / 나만의 스팟). */
enum class ArchiveTab(val title: String) {
    SavedSpots("저장된 스팟"),
    MySpots("나만의 스팟"),
}
