<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- GA(Firebase Analytics)는 이벤트 분석에만 쓰고 광고 ID 는 수집하지 않는다.
         play-services-measurement 가 자동 병합하는 AD_ID 권한을 제거해
         Play Data Safety "광고 ID 수집: 아니요" 선언과 실제 매니페스트를 일치시킨다. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />

    <!-- Android 11+ package visibility: 네이버 지도 nmap:// 길찾기 + Play Store + 공유 chooser, 다이얼러 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nmap" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <package android:name="com.nhn.android.nmap" />
    </queries>

    <application
        android:name=".app.PickflowApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pickflow">

        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="${naverMapClientId}" />
        <activity
            android:name=".app.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Pickflow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- iOS Universal Link (`pickflow-api.us/{SpotIDCoder-encoded}`) 1:1 — share URL 진입.
                 autoVerify 는 BE 가 .well-known/assetlinks.json 을 호스팅해야 활성화된다. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="pickflow-api.us" />
            </intent-filter>

            <!-- 커스텀 스킴 폴백 (`pickflow://spot/{token}`) — 공유 랜딩 페이지가
                 App Links 미검증 환경에서 앱을 열 때 사용. iOS URL 스킴 1:1. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pickflow" android:host="spot" />
            </intent-filter>
        </activity>

        <!-- Apple Sign In OAuth redirect 수신. Service ID / redirect URI 는
             secrets.properties (APPLE_SERVICE_ID / APPLE_REDIRECT_URI) 에 등록 후 동작. -->
        <activity
            android:name=".app.AppleCallbackActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="pickflow-api.us"
                      android:pathPrefix="/auth/apple" />
            </intent-filter>
        </activity>

        <!-- Kakao 로그인 리다이렉트 (스킴: kakao + Native App Key) -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="oauth"
                    android:scheme="kakao${kakaoNativeAppKey}" />
            </intent-filter>
        </activity>

        <!-- 카메라 촬영 결과를 카메라 앱과 공유하기 위한 FileProvider. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
