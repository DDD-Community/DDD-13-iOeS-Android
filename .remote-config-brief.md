# [PV-79] 신규 기능 안내 — Firebase Remote Config 전환 지시

iOS(PV-80)에서 먼저 구현한 Remote Config 제어를 안드로이드에 이식한다.
**같은 Firebase 프로젝트의 같은 파라미터를 iOS와 공유하므로, 아래 계약(키·스키마·판정 규칙)은 임의로 바꾸면 안 된다.**

iOS 레포는 이 워크트리에서 볼 수 없으므로 필요한 스펙을 아래에 전부 적었다. 추측하지 말고 이 문서를 계약서로 삼을 것.

---

## 0. 현재 상태와 목표

현재(`5271612 [PV-41]`): V2 안내 팝업이 **기기 로컬 플래그**로만 제어된다.

- `core/services/protocols/V2NoticeStore.kt` — `seen: StateFlow<Boolean>` / `setSeen()`
- `core/services/impl/PrefsV2NoticeStore.kt` — SharedPreferences 구현
- `feature/map/V2NoticeViewModel.kt` — `visible = seen.map { !it }`
- `feature/map/components/V2NoticePopup.kt` — UI
- `feature/devmode/DevModeScreen.kt` — 플래그를 되돌리는 개발용 스위치

목표: **노출 여부를 서버(Remote Config)가 정하고, "봤는지"는 기존처럼 로컬이 기억**하도록 바꾼다.
즉 `visible = (원격이 켜져 있음) && (아직 안 봤음)`.

기존 Dev Mode 스위치와 팝업 UI는 그대로 유지한다. 건드리는 건 "켤지 말지를 누가 정하는가" 뿐이다.

---

## 1. Remote Config 계약 (변경 금지)

| 항목 | 값 |
|---|---|
| 파라미터 키 | `new_feature_flags` |
| 타입 | JSON |

```json
{
  "features": [
    { "key": "v2_update_modal", "startAt": 1788220800000, "endAt": 1790812800000 },
    { "key": "spot_open_guide", "startAt": 1788220800000, "endAt": 1790812800000 },
    { "key": "home_new_badge",  "durationDays": 14 }
  ]
}
```

필드는 `key`만 필수, 나머지 셋은 전부 optional이다.

| 필드 | 타입 | 의미 |
|---|---|---|
| `key` | String | feature 식별자 |
| `startAt` | Long (ms epoch) | 노출 시작. **있으면 "런칭 기준" 판정으로 분기** |
| `endAt` | Long (ms epoch) | 노출 종료. 경계 **미포함** |
| `durationDays` | Int | 노출 지속 일수 |

**밀리초 epoch다. 초 단위로 다루지 말 것.**

### feature 키 3개

| 키 | iOS 화면 | 안드로이드 대응 |
|---|---|---|
| `v2_update_modal` | 앱 진입 전체화면 모달 ("V2 업데이트 안내" — 햇살·야경 필터 추가 / 스팟 공개) | 기존 `V2NoticePopup` |
| `spot_open_guide` | 보관함 > 나만의 스팟 탭 바텀시트 ("스팟 공개 OPEN!") | 대응 화면 있으면 연결, 없으면 **이번 범위에서 제외하고 키만 예약** |
| `home_new_badge` | 홈 지도 테마 필터 칩(햇살·야경)에 5×5 오렌지 점 | 대응 필터 UI 있으면 연결, 없으면 동일하게 보류 |

**우선순위: `v2_update_modal` 하나만 확실히 동작시키는 것.** 나머지 둘은 대응 UI가 이미 있을 때만 붙이고, 없으면 만들지 말고 보류하고 보고할 것.

---

## 2. 판정 규칙 (iOS와 반드시 동일하게)

`key`로 feature를 찾고 다음 순서로 판정한다. **feature가 배열에 없으면 `false`.**

```
if (startAt != null) {
    // 런칭 기준 — 전 유저 공통 스케줄
    val end = endAt ?: (durationDays?.let { startAt + it.days.inWholeMilliseconds })
    if (end == null) return false          // 둘 다 없으면 무기한이 아니라 "아예 꺼짐"
    return now in startAt until end        // startAt <= now < end
} else {
    // 유저(기기) 기준
    if (durationDays == null) return false
    val firstSeenAt = 저장돼 있으면 그 값, 없으면 now 를 저장하고 now
    return now - firstSeenAt < durationDays.days.inWholeMilliseconds
}
```

주의할 두 가지:
- `startAt`이 있으면 **`durationDays`는 `endAt`이 없을 때만 쓰인다.** `endAt`이 있으면 무시된다.
- 유저 기준의 `firstSeenAt`은 **최초 "판정" 시각**이지 최초 노출 시각이 아니다. 판정 함수 안에서 없으면 심는다.

---

## 3. 캐시와 폴백

- fetch 성공 시 config 원문을 로컬에 저장한다. **저장 키에 앱 버전(versionName)을 포함시킨다.** iOS 키 포맷: `newFeatureGuide.v2.remoteConfig.<앱버전>`. 안드로이드도 같은 성질(버전 올리면 초기화)을 갖게 할 것.
- **fetch 실패 시 예외를 삼키고 마지막 캐시로 판정한다.** 오프라인에서 팝업 판정이 깨지면 안 된다.
- in-app default(`setDefaultsAsync`)는 **등록하지 않는다.** 최초 fetch 전이나 파라미터 부재 시 전부 `false`(안전하게 꺼짐)가 되는 게 의도된 동작이다.
- "봤음" 플래그도 앱 버전 스코프로 둔다. iOS: `v2UpdateModalSeen.<앱버전>` (기기 단위, 계정 무관).

---

## 4. fetch 주기

```kotlin
minimumFetchIntervalInSeconds = if (BuildConfig.DEBUG) 0 else 3600
```

DEBUG 0초는 QA가 Console에서 값 바꾸고 즉시 확인하기 위한 것이다. Release는 1시간.

---

## 5. 구현 지시

1. **의존성** — `firebase-bom 33.16.0`은 이미 있다. `libs.versions.toml`에 `firebase-config = { group = "com.google.firebase", name = "firebase-config" }` 추가하고 `app/build.gradle.kts`에 BOM 경유로 연결.

2. **Provider** — `core/services/protocols`에 인터페이스, `core/services/impl`에 Firebase 구현.
   - `suspend fun fetchFeatureConfig(): NewFeatureConfig` (`fetchAndActivate().await()`)
   - `fun activatedFeatureConfig(): NewFeatureConfig?` (네트워크 없이 이미 activate된 값)
   - JSON 파싱은 **kotlinx.serialization** (프로젝트 표준). `@Serializable data class` 두 개.
   - 테스트에서 갈아끼울 수 있게 인터페이스로 추상화할 것. Firebase를 직접 참조하는 클래스가 판정 로직을 갖지 않게 한다.

3. **Store 확장** — 기존 `V2NoticeStore`를 확장하거나 상위 스토어를 새로 두어, 노출 여부가 `원격 활성 && !seen`이 되게 한다.
   - 기존 `seen: StateFlow<Boolean>` / `setSeen()` / Dev Mode 스위치 동작은 **깨뜨리지 말 것.**
   - 저장은 기존 `PrefsV2NoticeStore`의 SharedPreferences 관례를 따른다(DataStore로 갈아엎지 말 것).

4. **DI** — `app/di/ServiceModule.kt`에 바인딩 추가. Hilt 관례는 기존 파일을 그대로 따를 것.

5. **갱신 시점** — 앱 진입 및 팝업이 뜨는 화면 진입 시 `fetchFeatureConfig()`를 호출하고, 결과가 오면 노출 상태를 재평가한다. `StateFlow`로 노출되어 있으니 fetch 완료 후 값이 자연히 갱신되게 설계할 것.

---

## 6. 함정 — iOS에서 실제로 터진 것

**노출 평가를 로그인 분기 안에 넣지 마라.**

iOS는 `if (로그인됨) { 팝업 평가 }` 구조라 게스트 유저가 V2 모달을 영영 못 받는 버그가 났다(PR #77에서 리뷰 지적됨). V2 안내가 알리는 "햇살·야경 필터"는 로그인 여부와 무관하게 모두가 쓰는 기능이다.

**`v2_update_modal`은 비로그인 유저 포함 전체 대상이다.** 메인 화면에 도달했으면 로그인 상태와 무관하게 평가되어야 한다.

(참고: `spot_open_guide`는 다르다. 계정별로 "봤음"을 분리해야 하고 내 스팟 목록이 필요하므로 로그인이 본질적 전제다. 이건 로그인 게이팅이 맞다.)

---

## 7. 테스트 (필수)

`app/src/test/java/com/pickflow/android/core` 아래에 판정 로직 단위 테스트. Firebase 의존 없이 fake provider로 돌 것.

최소 케이스 6개:
1. fetch 전에는 꺼져 있고, fetch 후 활성 구간이면 켜진다
2. 런칭 기준: `startAt` 시점 켜짐, `endAt` 시점 꺼짐(경계 미포함)
3. `setSeen(true)` 후에는 원격이 켜져 있어도 안 뜬다
4. 유저 기준(`startAt` 없음 + `durationDays`): 최초 판정 시각 저장, 기간 경과 후 꺼짐
5. fetch 실패 시 마지막 캐시로 판정한다
6. `startAt`은 있는데 `endAt`·`durationDays`가 둘 다 없으면 꺼진다

**비로그인 상태에서도 `v2_update_modal`이 평가되는지 확인하는 테스트를 반드시 포함할 것.** iOS가 놓친 지점이다.

---

## 8. 검증

DEBUG 빌드로 실기기/에뮬레이터에서:
1. Firebase Console → Remote Config → `new_feature_flags` 편집 후 **게시(Publish)**
2. 앱 재진입 (DEBUG는 fetch interval 0)
3. 값을 활성 구간 밖으로 바꿔 재게시 → 팝업이 사라지는지
4. 앱 삭제 후 재설치 → "봤음" 초기화되어 다시 뜨는지
5. **로그아웃/게스트 상태에서도 뜨는지** (6절)

진단용 로그는 `BuildConfig.DEBUG` 가드 안에서만 남기고, 최소한 아래 세 가지는 찍을 것:
- fetch 상태와 소스 (`fetchAndActivate` 결과)
- 받아온 `new_feature_flags` 원문
- feature별 `isActive` / `hasSeen` / 최종 노출 여부

iOS에서 이 세 줄 덕분에 "안 뜨는 이유가 기간인지, 이미 봤는지, 아예 평가를 안 한 건지"를 즉시 구분할 수 있었다.

---

## 9. 완료 보고

- 변경 파일 목록과 각 파일이 맡은 역할
- 추가한 테스트와 실행 결과
- `spot_open_guide` / `home_new_badge`를 붙였는지, 보류했다면 그 이유(대응 UI 부재 등)
- 계약(1~4절)에서 벗어난 부분이 있다면 무엇을 왜
